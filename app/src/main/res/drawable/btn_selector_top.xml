<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el botón está presionado -->
    <item android:state_pressed="true" android:color="@color/teal_700" />
    <!-- Estado cuando el botón está seleccionado -->
    <item android:state_selected="true" android:color="@color/teal_200" />
    <!-- Estado por defecto -->
    <item android:color="@android:color/holo_blue_dark" />
</selector>